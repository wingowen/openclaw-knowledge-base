
## NanoBOT HTTP API + StepFun 调试结论（durable）
- 用户明确要求：优先用 opencode（省钱）继续修 bug。
- 本次排查主线：`/v1/chat/completions` 在 StepFun（OpenRouter）下出现间歇性空 content。
- 关键修复方向：`nanobot/providers/litellm_provider.py` 的 `_parse_response` 增加/确认 fallback，兼容 `reasoning_content` 与 `reasoning` 字段。
- 已完成结构性修正：
  - `http_api/routers/chat_router.py` 支持按请求覆盖 model（不再只是回显）。
  - `http_api/core/app_state.py` 调整为优先使用 HTTP API 自身 settings/env（避免误读默认配置导致模型漂移）。
  - `Dockerfile.http` 已确保覆盖复制 `nanobot` 与 `http_api`，避免仅复制 http_api 导致核心 provider 修复未进镜像。
- 实测现象：
  - 空串问题显著减少并可返回非空内容。
  - 对“只回复ok”这类强约束指令，模型仍有“解释性输出”波动（属于行为稳定性问题，不等于空串 bug）。
- 当前可用模型链路：`openrouter/stepfun/step-3.5-flash:free`（容器与接口可通）。
- 后续建议（如继续迭代）：
  1) 在 HTTP 层增加强约束输出后处理（regex/模板裁剪）；
  2) 固定低温与短 max_tokens 做行为收敛；
  3) 保留最小回滚方案与 git diff 清单便于提交。
