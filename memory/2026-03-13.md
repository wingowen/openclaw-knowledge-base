
## 23:58 新概念英语第二册（Supabase）状态校准
- 重新核验结果（以 `articles` + `article_tags` 实际查询为准）：
  - `2-001 ~ 2-096` 课次覆盖完整（缺失 0）。
  - 二册文章总数 181（存在重复导入）。
  - 唯一课次 96；重复课次 82 个（多课次存在 2 条或以上记录）。
  - `article_tags` 中 `tag_id=7` 对应二册文章关联已覆盖（linked 181, unlinked 0）。
- 经验修正：
  - 不能用 `articles.tag_ids` 判断最终标签关联，需以 `article_tags` 关联表为准。
  - 先前“标签全未关联”的结论属于查询口径错误（字段/过滤写法导致）。
- 待办：执行去重清理（每课保留 1 篇优先保留句子完整记录），随后做一次完整性复检并输出最终清单。

## 23:59 新概念英语第二册去重完成
- 已执行数据库去重：每课仅保留 1 篇（按句子数优先，其次 total_sentences，再次 id）。
- 去重前：二册文章 181，唯一课次 96，重复记录 85。
- 去重后：二册文章 96，唯一课次 96，缺失课次 0。
- 标签关联：`tag_id=7` 与二册文章关联 96/96。
- 删除执行结果：删除重复文章 85（成功 85，失败 0）。
- 备份清单：`/root/.openclaw/workspace/memory/nce2_dedup_backup_2026-03-13.json`（含 keep/remove 明细）。
