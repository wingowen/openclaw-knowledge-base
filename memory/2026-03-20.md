# 2026-03-20

## 右侧短线候选观察名单自动化（放宽+技术指标复核）
- 用户明确要求：每天开盘前发送“候选观察名单”到 QQ 邮箱，并采用“放宽但含技术指标复核”口径。
- 已建立自动链路：
  1) `scripts/generate_preopen_watchlist.py` 生成候选观察名单
  2) `scripts/run_preopen_watchlist_mail.sh` 调用邮件脚本发送
  3) cron 定时（工作日）：`25 9 * * 1-5 /bin/bash /root/.openclaw/workspace/scripts/run_preopen_watchlist_mail.sh >> /root/.openclaw/workspace/cron_preopen_watchlist.log 2>&1`
- 已成功多次发送测试邮件到：`1318263468@qq.com`。

## 一致性与内容增强
- 修复“聊天内容与邮件内容不一致”问题：统一使用同一份生成文件作为聊天/邮件来源，避免快照时间与口径漂移。
- 邮件内容新增：
  - 每个候选票的买卖点：理想买点(MA5)、次优买点(MA10)、止损位、目标位
  - 触发条件与失效条件
  - 生成过程简版说明

## 过程可追踪明细（用户新增要求）
- 用户要求“每一步获取了多少、剔除了多少”的具体数量。
- 已在报告中新增“生成过程明细（数量追踪）”，包含：
  1) 主线板块获取数量
  2) 原始成分股、剔除非主板、剔除ST/退市、保留数量
  3) 去重后数量与去重剔除数量
  4) 技术指标计算输入数、失败数、长度不足数、成功数
  5) 最终候选数与筛选剔除数

## 稳定性优化
- 发现主数据源（EM）在盘中存在失败波动，导致候选可能异常偏少。
- 已加数据源兜底：`ak.stock_zh_a_hist` 失败时自动回退 `ak.stock_zh_a_hist_tx`（腾讯历史数据）并统一字段。
- 过程明细同步增加回退统计：主源失败数、腾讯回退成功数、腾讯回退失败数。
- 优化后样例结果（12:09 版本）：
  - 输入 264 只
  - 主源失败 8 只
  - 腾讯回退成功 255 只
  - 成功计算 256 只
  - 最终候选 10 只

## 用户偏好（本任务新增）
- 偏好可执行、可追踪的盘前邮件：不只给名单，还要给买卖点与量化过程明细，便于后续调参。

## 候选票追踪系统（2026-03-20 完成）
- 系统已完成开发并可直接使用
- 核心组件：
  - `scripts/watchlist_tracker.py`：状态管理 + `update-status` 子命令
  - `scripts/watchlist_dashboard.py`：Flask Web 看板（http://localhost:5000）
  - `scripts/watchlist_tracker_README.md`：完整运行说明
- 数据存储：SQLite `data/watchlist_tracker.db`
- 当日三仓数据已入库：观察10 / 确认1 / 进攻1
- 依赖已安装：Flask
- 可用命令：
  - `python3 scripts/watchlist_tracker.py update-status --date 2026-03-20 --bucket 进攻 --code 600310 --status 已入场 --note "测试"`
  - `python3 scripts/watchlist_dashboard.py` 启动看板
- 技术实现：opencode 子代理开发 + 本地验证通过
