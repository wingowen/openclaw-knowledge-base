# 2026-03-18

## 19:19 模型切换
- 用户要求切换为 Codex。
- 已通过 `session_status` 将会话模型切换为 `openai/gpt-5.3-codex`。

## 19:23-19:26 每日复盘邮件修正
- 用户反馈复盘邮件存在严重数据错误（将“报告当天收盘”误当“次日实际结果”）。
- 重新执行 `scripts/generate_daily_report.py --date 2026-03-18` 并成功发送修正邮件到 `1318263468@qq.com`。
- 修正后关键复盘结论：
  - 2026-03-17 创业板预判 3255~3305
  - 2026-03-18 实际 3346.37
  - 正确标记为“⚠️ 超出区间”。

## 20:23-20:35 daily_stock 本地/云端去重与恢复
- 先确认状态：
  - 本地 Web 存活（`http://127.0.0.1:8000/` 返回 200）
  - 本地 schedule 曾在运行
  - GitHub Actions 最近 schedule 成功。
- 按用户同意，先做过一次“去重”：
  - 移除本地 cron: `10 15 * * 1-5 /bin/bash /root/.openclaw/workspace/scripts/run_daily_report.sh`
  - 停止本地 `python3 main.py --schedule --no-notify`
  - 保留 `--webui-only`。

## 20:52-20:57 改为“本地+云端都跑”并增加校验
- 用户要求恢复双端同时运行并做校验。
- 已恢复本地定时：
  - `10 15 * * 1-5 /bin/bash /root/.openclaw/workspace/scripts/run_daily_report.sh`
- 已重启本地调度进程：
  - `python3 main.py --schedule --no-notify`
- 保留本地 Web：
  - `python3 main.py --webui-only --no-notify`
- 新增校验脚本：
  - `/root/.openclaw/workspace/daily_stock_analysis/scripts/validate_dual_run.sh`
  - 功能：校验本地报告/本地DB记录 + GitHub schedule run 成功 + artifact 包含当天报告。
- 实测（2026-03-18）校验结果：PASS。

## 关键认知（供后续避免误会）
- GitHub Actions 确实保留历史（run 日志 + artifact，当前 retention 30 天）。
- 但云端 run 不会自动回写本地 `data/stock_analysis.db`；若只跑云端，本地 Web 将逐渐无新数据。
- 当前已按用户要求采用“双端都跑 + 跑后校验”。
